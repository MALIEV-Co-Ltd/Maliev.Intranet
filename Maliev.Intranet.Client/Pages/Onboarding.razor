@page "/hr/onboarding"
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects legacy /hr/onboarding route to the consolidated Leave and Onboarding page (Onboarding tab).</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/hr/leave?tab=onboarding", replace: true);
    }
}

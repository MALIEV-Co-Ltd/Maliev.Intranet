@page "/hr/employees/{Id:guid}"
@using Maliev.Intranet.Shared
@using Maliev.Intranet.Shared.Dtos
@inject HttpClient Http
@inject ISnackbar Snackbar
@inject NavigationManager Navigation
@inject IDialogService DialogService

<PageTitle>Employee Details - Maliev Intranet</PageTitle>

@if (_isLoading)
{
    <MudProgressLinear Indeterminate="true" Color="Color.Primary" Class="mb-4" />
}
else if (_employee == null)
{
    <MudAlert Severity="Severity.Error">Employee not found.</MudAlert>
    <MudButton Variant="Variant.Text" StartIcon="@Icons.Material.Filled.ArrowBack" OnClick="@(() => Navigation.NavigateTo("/hr/employees"))">
        Back to Directory
    </MudButton>
}
else
{
    <MudBreadcrumbs Items="_breadcrumbs" Class="mb-4" />

    <MudPaper Elevation="0" Class="pa-4 mb-4">
        <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="4">
                <MudAvatar Size="Size.Large" Color="Color.Primary">@_employee.FirstName[0]</MudAvatar>
                <div>
                        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                            <MudText Typo="Typo.h5">@_employee.FullName</MudText>
                            <MudChip T="string" Size="Size.Small" Color="@GetStatusColor(_employee.EmploymentStatus)">
                                @_employee.EmploymentStatus
                            </MudChip>
                        </MudStack>
                        <MudText Typo="Typo.subtitle1" Class="mud-text-secondary">@_employee.JobTitle • @_employee.DepartmentName</MudText>
                </div>
            </MudStack>

            <MudStack Row="true" Spacing="2">
                <MudButton Variant="Variant.Filled" 
                           Color="Color.Primary" 
                           StartIcon="@Icons.Material.Outlined.Edit"
                           OnClick="@OpenEditDialog">
                    Edit Profile
                </MudButton>
                @if (_employee.EmploymentStatus == "Active")
                {
                    <MudButton Variant="Variant.Outlined" 
                               Color="Color.Error" 
                               StartIcon="@Icons.Material.Outlined.PersonOff"
                               OnClick="@TerminateEmployee">
                        Terminate
                    </MudButton>
                }
            </MudStack>
        </MudStack>
    </MudPaper>

    <MudTabs Elevation="0" Rounded="true" Border="true" ApplyEffectsToContainer="true" Class="mt-4">
        <MudTabPanel Text="Personal Info" Icon="@Icons.Material.Outlined.Person">
            <MudGrid Class="pa-4">
                <MudItem xs="12" md="6">
                    <MudPaper Elevation="0" Class="pa-4 border mud-border-lines-default rounded-lg">
                        <MudText Typo="Typo.h6" Class="mb-3">Employment Details</MudText>
                        <MudList T="string">
                            <MudListItem T="string" Text="Employee ID" SecondaryText="@_employee.Id.ToString()" Icon="@Icons.Material.Outlined.Badge" />
                            <MudListItem T="string" Text="Hire Date" SecondaryText="@(_employee.StartDate.ToString("dd MMM yyyy"))" Icon="@Icons.Material.Outlined.Event" />
                            <MudListItem T="string" Text="Manager" SecondaryText="@(_employee.ManagerName ?? "N/A")" Icon="@Icons.Material.Outlined.AccountTree" />
                            <MudListItem T="string" Text="Location" SecondaryText="@(_employee.WorkLocation ?? "N/A")" Icon="@Icons.Material.Outlined.LocationOn" />
                            <MudListItem T="string" Text="Type" SecondaryText="@(_employee.EmploymentType ?? "N/A")" Icon="@Icons.Material.Outlined.WorkOutline" />
                        </MudList>
                    </MudPaper>
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudPaper Elevation="0" Class="pa-4 border mud-border-lines-default rounded-lg">
                        <MudText Typo="Typo.h6" Class="mb-3">Contact Information</MudText>
                        <MudList T="string">
                            <MudListItem T="string" Text="Email" SecondaryText="@_employee.WorkEmail" Icon="@Icons.Material.Outlined.Email" />
                            <MudListItem T="string" Text="Phone" SecondaryText="@(_employee.MobilePhone ?? "N/A")" Icon="@Icons.Material.Outlined.Phone" />
                        </MudList>
                        
                        <MudDivider Class="my-3" />
                        <MudText Typo="Typo.subtitle1" Class="mb-2">Emergency Contacts</MudText>
                        @if (_employee.EmergencyContacts.Any())
                        {
                            @foreach (var contact in _employee.EmergencyContacts)
                            {
                                <MudStack Spacing="1" Class="mb-2 pa-2 border rounded-lg mud-border-lines-default">
                                    <MudText Typo="Typo.body2"><strong>@contact.Name</strong> (@contact.Relationship)</MudText>
                                    <MudText Typo="Typo.caption"><MudIcon Icon="@Icons.Material.Outlined.Phone" Size="Size.Small" /> @contact.Phone</MudText>
                                </MudStack>
                            }
                        }
                        else
                        {
                            <MudText Typo="Typo.caption" Class="mud-text-secondary">No emergency contacts listed.</MudText>
                        }
                    </MudPaper>
                </MudItem>
            </MudGrid>
        </MudTabPanel>

        <MudTabPanel Text="Teams" Icon="@Icons.Material.Outlined.Groups">
            <MudPaper Elevation="0" Class="pa-4">
                <MudText Typo="Typo.body2" Align="Align.Center" Class="pa-4 mud-text-secondary">Team assignments integration pending.</MudText>
            </MudPaper>
        </MudTabPanel>

        <MudTabPanel Text="Notes" Icon="@Icons.Material.Outlined.StickyNote2">
            <MudPaper Elevation="0" Class="pa-4">
                <MudText Typo="Typo.body2" Align="Align.Center" Class="pa-4 mud-text-secondary">Internal notes integration pending.</MudText>
            </MudPaper>
        </MudTabPanel>

        <MudTabPanel Text="Employment History" Icon="@Icons.Material.Outlined.WorkHistory">
            <MudPaper Elevation="0" Class="pa-4">
                <MudText Typo="Typo.body2" Align="Align.Center" Class="pa-4 mud-text-secondary">Employment history integration pending.</MudText>
            </MudPaper>
        </MudTabPanel>

        <MudTabPanel Text="Documents" Icon="@Icons.Material.Outlined.FolderOpen">
            <MudPaper Elevation="0" Class="pa-4">
                <MudText Typo="Typo.body2" Align="Align.Center" Class="pa-4 mud-text-secondary">Document management integration pending.</MudText>
            </MudPaper>
        </MudTabPanel>
    </MudTabs>
}

@code {
    [Parameter]
    public Guid Id { get; set; }

    private bool _isLoading = true;
    private EmployeeDetailDto? _employee;
    private List<BreadcrumbItem> _breadcrumbs = [];

    protected override async Task OnInitializedAsync()
    {
        await LoadEmployee();

        _breadcrumbs = 
        [
            new BreadcrumbItem("Home", href: "/"),
            new BreadcrumbItem("HR", href: null, disabled: true),
            new BreadcrumbItem("Employees", href: "/hr/employees"),
            new BreadcrumbItem(_employee?.FullName ?? "Detail", href: null, disabled: true)
        ];
    }

    private async Task LoadEmployee()
    {
        try
        {
            _isLoading = true;
            _employee = await Http.GetFromJsonAsync<EmployeeDetailDto>($"api/employees/{Id}");
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error loading employee: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isLoading = false;
        }
    }

    private async Task OpenEditDialog()
    {
        if (_employee == null) return;

        var parameters = new DialogParameters<EditEmployeeDialog>
        {
            { x => x.EmployeeId, Id },
            { x => x.Employee, _employee }
        };
        var dialog = await DialogService.ShowAsync<EditEmployeeDialog>("Edit Employee Profile", parameters);
        var result = await dialog.Result;
        if (result is { Canceled: false })
        {
            await LoadEmployee();
        }
    }

    private async Task TerminateEmployee()
    {
        if (_employee == null) return;

        bool? result = await DialogService.ShowMessageBoxAsync(
            "Terminate Employment",
            $"Are you sure you want to terminate the employment of {_employee.FullName}?",
            yesText: "Terminate", cancelText: "Cancel");

        if (result == true)
        {
            try
            {
                var request = new TerminateEmployeeRequest { TerminationDate = DateTime.Today, Reason = "User request" };
                var response = await Http.PostAsJsonAsync($"api/employees/{Id}/terminate", request);
                if (response.IsSuccessStatusCode)
                {
                    Snackbar.Add("Employment terminated", Severity.Warning);
                    await LoadEmployee();
                }
                else
                {
                    Snackbar.Add("Failed to terminate employment", Severity.Error);
                }
            }
            catch (Exception ex)
            {
                Snackbar.Add($"Error: {ex.Message}", Severity.Error);
            }
        }
    }

    private async Task AddNote()
    {
        // Feature disabled due to schema mismatch
        await Task.CompletedTask;
    }

    private Color GetStatusColor(string status) => status switch
    {
        "Active" => Color.Success,
        "OnLeave" => Color.Warning,
        "Terminated" => Color.Error,
        "Probation" => Color.Info,
        _ => Color.Default
    };
}

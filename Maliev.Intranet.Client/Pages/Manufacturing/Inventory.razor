@page "/mfg/inventory"
<PageTitle>Inventory | Intranet Gateway</PageTitle>
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects legacy /mfg/inventory route to the consolidated Materials and Inventory page (Inventory tab).</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/mfg/materials?tab=inventory", replace: true);
    }
}

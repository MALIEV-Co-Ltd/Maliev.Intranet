@page "/sales/customers/{Id:guid}"
@namespace Maliev.Intranet.Client.Pages.Legacy
@inject NavigationManager Navigation

@code {
    [Parameter] public Guid Id { get; set; }

    protected override void OnInitialized() => Navigation.NavigateTo($"/customers/{Id}", replace: true);
}

@page "/sales/customers/new"
@namespace Maliev.Intranet.Client.Pages.Legacy
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/customers/new", replace: true);
}

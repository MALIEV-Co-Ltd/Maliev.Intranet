@page "/sales/customers"
@namespace Maliev.Intranet.Client.Pages.Legacy
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/customers", replace: true);
}

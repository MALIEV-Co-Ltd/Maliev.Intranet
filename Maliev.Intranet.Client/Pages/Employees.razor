@page "/hr/employees"
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects old /hr/employees route to the consolidated People Directory (Employee Management tab).</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/hr/directory?tab=management", replace: true);
    }
}

@page "/finance/report-builder"
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects legacy /finance/report-builder URL to the consolidated Accounting page (Report Builder tab).</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/finance/accounting?tab=builder", replace: true);
    }
}

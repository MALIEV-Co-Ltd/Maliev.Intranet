@page "/hr/time-off"
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects legacy /hr/time-off route to the consolidated Leave and Onboarding page.</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/hr/leave?tab=timeoff", replace: true);
    }
}

@page "/finance/ledger"
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects legacy /finance/ledger URL to the consolidated Accounting page.</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/finance/accounting?tab=ledger", replace: true);
    }
}

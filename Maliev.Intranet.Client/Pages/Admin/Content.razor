@page "/admin/content"
@inject NavigationManager Navigation

@code {
    /// <summary>Redirects legacy /admin/content route to the consolidated Settings page (Content tab).</summary>
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/admin/settings?tab=content", replace: true);
    }
}

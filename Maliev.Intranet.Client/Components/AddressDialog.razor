@using MudBlazor
@using Maliev.Intranet.Shared
@inject ISnackbar Snackbar

<MudDialog>
    <TitleContent>
        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
            <MudIcon Icon="@(Address.Type == "Billing" ? Icons.Material.Filled.ReceiptLong : Icons.Material.Filled.LocalShipping)" Color="Color.Primary" />
            <MudText Typo="Typo.h6">@(IsNew ? "Add" : "Edit") @Address.Type Address</MudText>
        </MudStack>
    </TitleContent>
    <DialogContent>
        <MudForm @ref="_form" Model="@Address">
            <MudStack Spacing="4">
                <!-- Type and Default Toggle -->
                <MudPaper Elevation="0" Class="pa-4 mud-bg-surface-light rounded-xl border-solid border-1 mud-border-lines-default">
                    <MudGrid Spacing="2" Justify="Justify.Center">
                        <MudItem xs="12" sm="7">
                            <MudStack Spacing="1">
                                <MudText Typo="Typo.caption" Style="font-weight: 700;" Color="Color.Default">ADDRESS TYPE</MudText>
                                <MudToggleGroup T="string" Value="@Address.Type" ValueChanged="@OnTypeChanged" 
                                                SelectionMode="SelectionMode.SingleSelection" Color="Color.Primary" 
                                                CheckMark="false" FixedContent="true" Class="rounded-lg">
                                    <MudToggleItem Value="@("Billing")" Text="Billing" UnselectedIcon="@Icons.Material.Filled.ReceiptLong" SelectedIcon="@Icons.Material.Filled.ReceiptLong" />
                                    <MudToggleItem Value="@("Shipping")" Text="Shipping" UnselectedIcon="@Icons.Material.Filled.LocalShipping" SelectedIcon="@Icons.Material.Filled.LocalShipping" />
                                </MudToggleGroup>
                            </MudStack>
                        </MudItem>
                        <MudItem xs="12" sm="5">
                            <MudStack AlignItems="AlignItems.Start" Spacing="0">
                                <MudText Typo="Typo.caption" Style="font-weight: 700;" Color="Color.Default">PRIMARY STATUS</MudText>
                                <MudSwitch T="bool" @bind-Value="Address.IsDefault" Color="Color.Success" 
                                           Label="@(Address.IsDefault ? $"Default {Address.Type}" : "Set as Default")" />
                            </MudStack>
                        </MudItem>
                    </MudGrid>
                </MudPaper>

                <GoogleAddressPicker Latitude="@Address.Latitude"
                                     Longitude="@Address.Longitude"
                                     FormattedAddress="@Address.FormattedAddress"
                                     SelectionChanged="ApplyGoogleSelection" />

                <MudGrid Spacing="3">
                    <MudItem xs="12">
                        <MudText Typo="Typo.subtitle2">Place label</MudText>
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudToggleGroup T="string" @bind-Value="Address.PlaceLabel"
                                        SelectionMode="SelectionMode.SingleSelection"
                                        Color="Color.Primary"
                                        CheckMark="false"
                                        FixedContent="true"
                                        Class="rounded-lg">
                            <MudToggleItem Value="@("Home")" Text="Home" UnselectedIcon="@Icons.Material.Filled.Home" SelectedIcon="@Icons.Material.Filled.Home" />
                            <MudToggleItem Value="@("Work")" Text="Work" UnselectedIcon="@Icons.Material.Filled.Business" SelectedIcon="@Icons.Material.Filled.Business" />
                            <MudToggleItem Value="@("Other")" Text="Other" UnselectedIcon="@Icons.Material.Filled.Place" SelectedIcon="@Icons.Material.Filled.Place" />
                        </MudToggleGroup>
                    </MudItem>
                    @if (string.Equals(Address.PlaceLabel, "Other", StringComparison.OrdinalIgnoreCase))
                    {
                        <MudItem xs="12" sm="6">
                            <MudTextField T="string" Label="Other place name" @bind-Value="Address.PlaceLabelOther" Immediate="true" Variant="Variant.Outlined" Margin="Margin.Dense" />
                        </MudItem>
                    }

                    @if (Address.Type == "Billing")
                    {
                        @if (HasCompany)
                        {
                            <MudItem xs="12">
                                <MudTextField T="string" Label="Company Name" Value="@CompanyName" Variant="Variant.Outlined" ReadOnly="true" Disabled="true" Margin="Margin.Dense" />
                            </MudItem>
                        }
                        else
                        {
                            <MudItem xs="12" sm="6">
                                <MudTextField T="string" Label="Customer Name" Value="@CustomerName" Variant="Variant.Outlined" ReadOnly="true" Disabled="true" Margin="Margin.Dense" />
                            </MudItem>
                            <MudItem xs="12" sm="6">
                                <MudTextField T="string" Label="Customer Mobile" Value="@CustomerMobile" Variant="Variant.Outlined" ReadOnly="true" Disabled="true" Margin="Margin.Dense" />
                            </MudItem>
                        }
                    }

                    <MudItem xs="12">
                        <MudText Typo="Typo.subtitle2">Contact Information</MudText>
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField T="string" Label="Recipient Name" @bind-Value="Address.RecipientName" Immediate="true" Variant="Variant.Outlined" Placeholder="@CustomerName" Margin="Margin.Dense" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField T="string" Label="Mobile Number" @bind-Value="Address.RecipientPhone" Immediate="true" Variant="Variant.Outlined" Placeholder="@CustomerMobile" Margin="Margin.Dense" />
                    </MudItem>
                    <MudItem xs="12">
                        <MudTextField T="string" Label="Note to driver" @bind-Value="Address.DriverNote" Immediate="true" Variant="Variant.Outlined" Margin="Margin.Dense" Lines="2" />
                    </MudItem>

                    <MudItem xs="12">
                        <MudTextField T="string" Label="Address No./Moo/Soi/Road" @bind-Value="Address.AddressLine1" Immediate="true" Variant="Variant.Outlined" Required="true" Placeholder="House/building number, street" Margin="Margin.Dense" />
                    </MudItem>

                    <MudItem xs="12" sm="6">
                        <MudTextField T="string" Label="Address Line 2 (Optional)" @bind-Value="Address.AddressLine2" Immediate="true" Variant="Variant.Outlined" Margin="Margin.Dense" />
                    </MudItem>
                    <MudItem xs="12" sm="6">
                        <MudTextField T="string" Label="Address Line 3 (Optional)" @bind-Value="Address.AddressLine3" Immediate="true" Variant="Variant.Outlined" Margin="Margin.Dense" />
                    </MudItem>

                    <MudItem xs="12" sm="6">
                        <MudAutocomplete T="RegistryThaiLocation"
                                         Label="Sub District"
                                         Value="@SelectedLocation"
                                         Text="@Address.District"
                                         TextChanged="@((string text) => Address.District = text)"
                                         SearchFunc="@SearchLocations"
                                         ToStringFunc="@GetLocationString"
                                         Variant="Variant.Outlined"
                                         Immediate="true"
                                         Clearable="true"
                                         CoerceText="false"
                                         CoerceValue="false"
                                         ResetValueOnEmptyText="false"
                                         DebounceInterval="500"
                                         MinCharacters="2"
                                         Margin="Margin.Dense"
                                         ValueChanged="@OnLocationSelected"
                                         Validation="@(new Func<RegistryThaiLocation?, string?>(v => string.IsNullOrEmpty(Address.District) ? "Required" : null))"
                                        >
                            <ItemTemplate Context="loc">
                                <div class="py-1">
                                    <MudText Typo="Typo.body2" Style="font-weight: 500;">@($"{loc.SubDistrictTh} {loc.DistrictTh} {loc.ProvinceTh} {loc.PostalCode}")</MudText>
                                    <MudText Typo="Typo.caption" Color="Color.Default">@($"{loc.SubDistrictEn}, {loc.DistrictEn}, {loc.ProvinceEn} {loc.PostalCode}")</MudText>
                                </div>
                            </ItemTemplate>
                        </MudAutocomplete>
                    </MudItem>

                    <MudItem xs="12" sm="6">
                        <MudAutocomplete T="RegistryThaiLocation"
                                         Label="District"
                                         Value="@SelectedLocation"
                                         Text="@Address.City"
                                         TextChanged="@((string text) => Address.City = text)"
                                         SearchFunc="@SearchLocations"
                                         ToStringFunc="@GetCityString"
                                         Variant="Variant.Outlined"
                                         Immediate="true"
                                         Margin="Margin.Dense"
                                         Validation="@(new Func<RegistryThaiLocation?, string?>(v => string.IsNullOrEmpty(Address.City) ? "Required" : null))"
                                         Clearable="true"
                                         CoerceText="false"
                                         CoerceValue="false"
                                         ResetValueOnEmptyText="false"
                                         DebounceInterval="500"
                                         MinCharacters="2"
                                         ValueChanged="@OnLocationSelected"
                                        >
                            <ItemTemplate Context="loc">
                                <div class="py-1">
                                    <MudText Typo="Typo.body2" Style="font-weight: 500;">@($"{loc.SubDistrictTh} {loc.DistrictTh} {loc.ProvinceTh} {loc.PostalCode}")</MudText>
                                    <MudText Typo="Typo.caption" Color="Color.Default">@($"{loc.SubDistrictEn}, {loc.DistrictEn}, {loc.ProvinceEn} {loc.PostalCode}")</MudText>
                                </div>
                            </ItemTemplate>
                        </MudAutocomplete>
                    </MudItem>

                    <MudItem xs="12" sm="6">
                        <MudAutocomplete T="RegistryThaiLocation"
                                         Label="Province"
                                         Value="@SelectedLocation"
                                         Text="@Address.StateProvince"
                                         TextChanged="@((string text) => Address.StateProvince = text)"
                                         SearchFunc="@SearchLocations"
                                         ToStringFunc="@GetProvinceString"
                                         Variant="Variant.Outlined"
                                         Immediate="true"
                                         Margin="Margin.Dense"
                                         Validation="@(new Func<RegistryThaiLocation?, string?>(v => string.IsNullOrEmpty(Address.StateProvince) ? "Required" : null))"
                                         Clearable="true"
                                         CoerceText="false"
                                         CoerceValue="false"
                                         ResetValueOnEmptyText="false"
                                         DebounceInterval="500"
                                         MinCharacters="2"
                                         Disabled="@ProvinceLocked"
                                         ValueChanged="@OnLocationSelected"
                                        >
                            <ItemTemplate Context="loc">
                                <div class="py-1">
                                    <MudText Typo="Typo.body2" Style="font-weight: 500;">@($"{loc.SubDistrictTh} {loc.DistrictTh} {loc.ProvinceTh} {loc.PostalCode}")</MudText>
                                    <MudText Typo="Typo.caption" Color="Color.Default">@($"{loc.SubDistrictEn}, {loc.DistrictEn}, {loc.ProvinceEn} {loc.PostalCode}")</MudText>
                                </div>
                            </ItemTemplate>
                        </MudAutocomplete>
                    </MudItem>

                    <MudItem xs="12" sm="6">
                        <MudAutocomplete T="RegistryThaiLocation"
                                         Label="Postal Code"
                                         Value="@SelectedLocation"
                                         Text="@Address.PostalCode"
                                         TextChanged="@((string text) => Address.PostalCode = text)"
                                         SearchFunc="@SearchLocations"
                                         ToStringFunc="@(l => l?.PostalCode ?? (Address.PostalCode ?? string.Empty))"
                                         Variant="Variant.Outlined"
                                         Immediate="true"
                                         Margin="Margin.Dense"
                                         Validation="@(new Func<RegistryThaiLocation?, string?>(v => string.IsNullOrEmpty(Address.PostalCode) ? "Required" : null))"
                                         Clearable="true"
                                         CoerceText="false"
                                         CoerceValue="false"
                                         ResetValueOnEmptyText="false"
                                         DebounceInterval="500"
                                         MinCharacters="2"
                                         Disabled="@PostalCodeLocked"
                                         ValueChanged="@OnLocationSelected"
                                        >
                            <ItemTemplate Context="loc">
                                <div class="py-1">
                                    <MudText Typo="Typo.body2" Style="font-weight: 500;">@loc.PostalCode</MudText>
                                    <MudText Typo="Typo.caption" Color="Color.Default">@($"{loc.SubDistrictTh}, {loc.DistrictTh}, {loc.ProvinceTh}")</MudText>
                                </div>
                            </ItemTemplate>
                        </MudAutocomplete>
                    </MudItem>

                    <MudItem xs="12">
                        <MudAutocomplete T="CountryDto"
                                         Label="Country"
                                         Value="@SelectedCountry"
                                         SearchFunc="@SearchCountries"
                                         ToStringFunc="@(c => c?.Name ?? string.Empty)"
                                         Variant="Variant.Outlined"
                                         Immediate="true"
                                         Margin="Margin.Dense"
                                         Required="true"
                                         ValueChanged="@OnCountryChanged"
                                         />
                    </MudItem>
                </MudGrid>
            </MudStack>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Submit" Class="px-8">Save Changes</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; } = default!;
    [Parameter] public CreateAddressRequest Address { get; set; } = new();
    [Parameter] public bool IsNew { get; set; }
    [Parameter] public bool HasCompany { get; set; }
    [Parameter] public string? CompanyName { get; set; }
    [Parameter] public string? CustomerName { get; set; }
    [Parameter] public string? CustomerMobile { get; set; }
    [Parameter] public List<CountryDto> AllCountries { get; set; } = [];
    [Parameter] public Func<string?, CancellationToken, Task<IEnumerable<RegistryThaiLocation>>> SearchLocations { get; set; } = default!;

    private MudForm _form = default!;
    private CountryDto? SelectedCountry;
    private RegistryThaiLocation? SelectedLocation;
    private bool ProvinceLocked => IsGoogleAddress && !string.IsNullOrWhiteSpace(Address.StateProvince);
    private bool PostalCodeLocked => IsGoogleAddress && !string.IsNullOrWhiteSpace(Address.PostalCode);
    private bool IsGoogleAddress => string.Equals(Address.AddressSource, "GooglePlace", StringComparison.OrdinalIgnoreCase)
        || string.Equals(Address.AddressSource, "GoogleMapPin", StringComparison.OrdinalIgnoreCase);

    protected override void OnInitialized()
    {
        Address.PlaceLabel = string.IsNullOrWhiteSpace(Address.PlaceLabel) ? "Home" : Address.PlaceLabel;
        Address.RecipientName = string.IsNullOrWhiteSpace(Address.RecipientName) ? CustomerName : Address.RecipientName;
        Address.RecipientPhone = string.IsNullOrWhiteSpace(Address.RecipientPhone) ? CustomerMobile : Address.RecipientPhone;
        SelectedCountry = AllCountries.FirstOrDefault(c => c.Id == Address.CountryId);
        
        if (!string.IsNullOrEmpty(Address.District) || !string.IsNullOrEmpty(Address.City))
        {
            SelectedLocation = new RegistryThaiLocation
            {
                SubDistrictTh = Address.District ?? string.Empty,
                SubDistrictEn = Address.District ?? string.Empty,
                DistrictTh = Address.City ?? string.Empty,
                DistrictEn = Address.City ?? string.Empty,
                ProvinceTh = Address.StateProvince ?? string.Empty,
                ProvinceEn = Address.StateProvince ?? string.Empty,
                PostalCode = Address.PostalCode ?? string.Empty
            };
        }
    }

    private void ApplyGoogleSelection(GoogleAddressSelection selection)
    {
        if (!string.IsNullOrWhiteSpace(selection.AddressLine1)) Address.AddressLine1 = selection.AddressLine1;
        if (!string.IsNullOrWhiteSpace(selection.District)) Address.District = selection.District;
        if (!string.IsNullOrWhiteSpace(selection.City)) Address.City = selection.City;
        if (!string.IsNullOrWhiteSpace(selection.StateProvince)) Address.StateProvince = selection.StateProvince;
        if (!string.IsNullOrWhiteSpace(selection.PostalCode)) Address.PostalCode = selection.PostalCode;

        Address.AddressSource = string.IsNullOrWhiteSpace(selection.Source) ? "GooglePlace" : selection.Source;
        Address.GooglePlaceId = selection.PlaceId;
        Address.FormattedAddress = selection.FormattedAddress;
        Address.Latitude = selection.Latitude;
        Address.Longitude = selection.Longitude;

        var thai = AllCountries.FirstOrDefault(c => c.Code == "TH");
        if (thai != null) OnCountryChanged(thai);
    }

    private void OnTypeChanged(string type)
    {
        Address.Type = type;
    }

    private void OnLocationSelected(RegistryThaiLocation? location)
    {
        if (location == null) return;
        SelectedLocation = location;
        
        var useThai = IsThai(Address.AddressLine1) || IsThai(Address.District);
        
        Address.District = useThai ? location.SubDistrictTh : location.SubDistrictEn;
        Address.City = useThai ? location.DistrictTh : location.DistrictEn;
        Address.StateProvince = useThai ? location.ProvinceTh : location.ProvinceEn;
        Address.PostalCode = location.PostalCode;
        
        var thai = AllCountries.FirstOrDefault(c => c.Code == "TH");
        if (thai != null) OnCountryChanged(thai);
    }

    private void OnCountryChanged(CountryDto? country)
    {
        SelectedCountry = country;
        if (country != null) Address.CountryId = country.Id;
    }

    private Task<IEnumerable<CountryDto>> SearchCountries(string? query, CancellationToken ct)
    {
        if (string.IsNullOrEmpty(query)) return Task.FromResult<IEnumerable<CountryDto>>(AllCountries);
        return Task.FromResult(AllCountries.Where(c => c.Name.Contains(query, StringComparison.OrdinalIgnoreCase)));
    }

    private string GetLocationString(RegistryThaiLocation? l) => l != null ? (IsThai(Address.District) ? l.SubDistrictTh : l.SubDistrictEn) : (Address.District ?? string.Empty);
    private string GetCityString(RegistryThaiLocation? l) => l != null ? (IsThai(Address.City) ? l.DistrictTh : l.DistrictEn) : (Address.City ?? string.Empty);
    private string GetProvinceString(RegistryThaiLocation? l) => l != null ? (IsThai(Address.StateProvince) ? l.ProvinceTh : l.ProvinceEn) : (Address.StateProvince ?? string.Empty);

    private static bool IsThai(string? value) => value?.Any(c => c >= 0x0E00 && c <= 0x0E7F) ?? false;

    private async Task Submit()
    {
        await _form.ValidateAsync();
        if (_form.IsValid) MudDialog.Close(DialogResult.Ok(Address));
        else Snackbar.Add("Please fill in all required fields correctly.", Severity.Warning);
    }

    private void Cancel() => MudDialog.Cancel();
}

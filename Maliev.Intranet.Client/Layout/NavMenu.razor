@using MudBlazor
@using Maliev.Intranet.Client.Services
@using Maliev.Intranet.Shared
@using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager Navigation

<MudNavMenu Class="mud-width-full d-flex flex-column" Style="height: 100%; overflow: hidden;">
    <div class="flex-grow-1 custom-scrollbar" style="overflow-y: auto;">
        <MudTooltip Text="Dashboard" Placement="Placement.Right" Delay="300" Inline="false">
            <MudNavLink Href="" Match="NavLinkMatch.All" Icon="@Icons.Material.Outlined.Dashboard">Dashboard</MudNavLink>
        </MudTooltip>

        <!-- Sales & CRM -->
        <MudNavGroup Title="Sales & CRM" Icon="@Icons.Material.Outlined.Business" Expanded="false">
            <MudTooltip Text="@(HasPermission("customer.customers.read") ? "Customers" : "Customers (requires customer.customers.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="sales/customers" Icon="@Icons.Material.Outlined.People"
                            Style="@(HasPermission("customer.customers.read") ? "" : "opacity: 0.4; pointer-events: none;")">Customers</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("registry.companies.read") ? "Companies" : "Companies (requires registry.companies.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="sales/companies" Icon="@Icons.Material.Outlined.Business"
                            Style="@(HasPermission("registry.companies.read") ? "" : "opacity: 0.4; pointer-events: none;")">Companies</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("project.projects.read") ? "Projects" : "Projects (requires project.projects.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="sales/projects" Icon="@Icons.Material.Outlined.FolderSpecial"
                            Style="@(HasPermission("project.projects.read") ? "" : "opacity: 0.4; pointer-events: none;")">Projects</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("quotation.quotations.read") ? "Quotations" : "Quotations (requires quotation.quotations.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="sales/quotations" Icon="@Icons.Material.Outlined.Description"
                            Style="@(HasPermission("quotation.quotations.read") ? "" : "opacity: 0.4; pointer-events: none;")">Quotations</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("order.orders.read") ? "Orders" : "Orders (requires order.orders.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="sales/orders" Icon="@Icons.Material.Outlined.ShoppingCart"
                            Style="@(HasPermission("order.orders.read") ? "" : "opacity: 0.4; pointer-events: none;")">Orders</MudNavLink>
            </MudTooltip>
        </MudNavGroup>

        <!-- Manufacturing -->
        <MudNavGroup Title="Manufacturing" Icon="@Icons.Material.Outlined.PrecisionManufacturing" Expanded="false">
            <MudTooltip Text="@(HasPermission("material.materials.read") ? "Materials & Inventory" : "Materials & Inventory (requires material.materials.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="mfg/materials" Icon="@Icons.Material.Outlined.Inventory2"
                            Style="@(HasPermission("material.materials.read") ? "" : "opacity: 0.4; pointer-events: none;")">Materials &amp; Inventory</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("facility.equipments.read") ? "Equipment" : "Equipment (requires facility.equipments.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="mfg/equipment" Icon="@Icons.Material.Outlined.PrecisionManufacturing"
                            Style="@(HasPermission("facility.equipments.read") ? "" : "opacity: 0.4; pointer-events: none;")">Equipment</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("job.jobs.read") ? "Production Queue" : "Production Queue (requires job.jobs.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="mfg/production-queue" Icon="@Icons.Material.Outlined.Factory"
                            Style="@(HasPermission("job.jobs.read") ? "" : "opacity: 0.4; pointer-events: none;")">Production Queue</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("purchase-order.orders.read") ? "Procurement & Suppliers" : "Procurement & Suppliers (requires purchase-order.orders.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="mfg/procurement" Icon="@Icons.Material.Outlined.ShoppingBag"
                            Style="@(HasPermission("purchase-order.orders.read") ? "" : "opacity: 0.4; pointer-events: none;")">Procurement &amp; Suppliers</MudNavLink>
            </MudTooltip>
        </MudNavGroup>

        <!-- Finance -->
        <MudNavGroup Title="Finance" Icon="@Icons.Material.Outlined.AccountBalanceWallet" Expanded="false">
            <MudTooltip Text="@(HasPermission("invoice.invoices.read") ? "Invoices" : "Invoices (requires invoice.invoices.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="finance/invoices" Icon="@Icons.Material.Outlined.Receipt"
                            Style="@(HasPermission("invoice.invoices.read") ? "" : "opacity: 0.4; pointer-events: none;")">Invoices</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("payment.payments.read") ? "Payments & Receipts" : "Payments & Receipts (requires payment.payments.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="finance/payments" Icon="@Icons.Material.Outlined.Payments"
                            Style="@(HasPermission("payment.payments.read") ? "" : "opacity: 0.4; pointer-events: none;")">Payments &amp; Receipts</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("delivery.delivery-notes.read") ? "Delivery Notes" : "Delivery Notes (requires delivery.delivery-notes.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="finance/delivery-notes" Icon="@Icons.Material.Outlined.LocalShipping"
                            Style="@(HasPermission("delivery.delivery-notes.read") ? "" : "opacity: 0.4; pointer-events: none;")">Delivery Notes</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("accounting.accounts.read") ? "Accounting" : "Accounting (requires accounting.accounts.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="finance/accounting" Icon="@Icons.Material.Outlined.AccountBalance"
                            Style="@(HasPermission("accounting.accounts.read") ? "" : "opacity: 0.4; pointer-events: none;")">Accounting</MudNavLink>
            </MudTooltip>
        </MudNavGroup>

        <!-- HR & People -->
        <MudNavGroup Title="HR & People" Icon="@Icons.Material.Outlined.Groups" Expanded="false">
            <MudTooltip Text="@(HasPermission("employee.employees.read") ? "People Directory" : "People Directory (requires employee.employees.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="hr/directory" Icon="@Icons.Material.Outlined.ContactPage"
                            Style="@(HasPermission("employee.employees.read") ? "" : "opacity: 0.4; pointer-events: none;")">People Directory</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("career.job-postings.read") ? "Recruitment" : "Recruitment (requires career.job-postings.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="hr/recruitment" Icon="@Icons.Material.Outlined.WorkOutline"
                            Style="@(HasPermission("career.job-postings.read") ? "" : "opacity: 0.4; pointer-events: none;")">Recruitment</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("leave.balances.read") ? "Leave & Onboarding" : "Leave & Onboarding (requires leave.balances.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="hr/leave" Icon="@Icons.Material.Outlined.EventBusy"
                            Style="@(HasPermission("leave.balances.read") ? "" : "opacity: 0.4; pointer-events: none;")">Leave &amp; Onboarding</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("performance.reviews.read") ? "Training & Performance" : "Training & Performance (requires performance.reviews.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="hr/training" Icon="@Icons.Material.Outlined.School"
                            Style="@(HasPermission("performance.reviews.read") ? "" : "opacity: 0.4; pointer-events: none;")">Training &amp; Performance</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("compensation.salaries.read") ? "Compensation" : "Compensation (requires compensation.salaries.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="hr/compensation" Icon="@Icons.Material.Outlined.MonetizationOn"
                            Style="@(HasPermission("compensation.salaries.read") ? "" : "opacity: 0.4; pointer-events: none;")">Compensation</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("compliance.records.read") ? "Compliance & Analytics" : "Compliance & Analytics (requires compliance.records.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="hr/compliance" Icon="@Icons.Material.Outlined.FactCheck"
                            Style="@(HasPermission("compliance.records.read") ? "" : "opacity: 0.4; pointer-events: none;")">Compliance &amp; Analytics</MudNavLink>
            </MudTooltip>
        </MudNavGroup>

        <!-- Operations -->
        <MudNavGroup Title="Operations" Icon="@Icons.Material.Outlined.AdminPanelSettings" Expanded="false">
            <MudTooltip Text="@(HasPermission("iam.manage") ? "IAM Console" : "IAM Console (requires iam.manage)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="admin/iam" Icon="@Icons.Material.Outlined.Security"
                            Style="@(HasPermission("iam.manage") ? "" : "opacity: 0.4; pointer-events: none;")">IAM Console</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("system.health.read") ? "System Health" : "System Health (requires system.health.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="admin/system-health" Icon="@Icons.Material.Outlined.HealthAndSafety"
                            Style="@(HasPermission("system.health.read") ? "" : "opacity: 0.4; pointer-events: none;")">System Health</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("iam.read") ? "Audit Log" : "Audit Log (requires iam.read)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="admin/audit" Icon="@Icons.Material.Outlined.History"
                            Style="@(HasPermission("iam.read") ? "" : "opacity: 0.4; pointer-events: none;")">Audit Log</MudNavLink>
            </MudTooltip>
            <MudTooltip Text="@(HasPermission("iam.manage") ? "Settings" : "Settings (requires iam.manage)")" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink Href="admin/settings" Icon="@Icons.Material.Outlined.Settings"
                            Style="@(HasPermission("iam.manage") ? "" : "opacity: 0.4; pointer-events: none;")">Settings</MudNavLink>
            </MudTooltip>
        </MudNavGroup>
    </div>

    <div class="mt-auto sticky-bottom-logout">
        <MudDivider />
        <div class="pa-2">
            <MudTooltip Text="Logout" Placement="Placement.Right" Delay="300" Inline="false">
                <MudNavLink OnClick="@HandleLogout" 
                            Icon="@Icons.Material.Outlined.DoorBack" 
                            IconColor="Color.Inherit"
                            Class="rounded-lg logout-nav-link">
                    Logout
                </MudNavLink>
            </MudTooltip>
        </div>
    </div>
</MudNavMenu>

<style>
    .logout-nav-link {
        background-color: var(--mud-palette-error) !important;
        color: white !important;
    }
    
    .logout-nav-link:hover {
        background-color: var(--mud-palette-error-darken) !important;
    }

    .logout-nav-link .mud-nav-link-text {
        font-weight: 500;
    }

    .sticky-bottom-logout {
        background-color: var(--mud-palette-drawer-background);
        z-index: 10;
    }

    /* Fix NavLink width issue caused by Tooltip wrapping */
    .mud-tooltip-root {
        width: 100%;
    }

    .custom-scrollbar::-webkit-scrollbar {
        width: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: var(--mud-palette-divider);
        border-radius: 4px;
    }
</style>

@code {
    [CascadingParameter]
    private Task<AuthenticationState> AuthStateTask { get; set; } = null!;

    private System.Security.Claims.ClaimsPrincipal _user = new();

    /// <summary>
    /// Loads the authenticated user's claims on component initialization.
    /// </summary>
    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthStateTask;
        _user = authState.User;
    }

    /// <summary>
    /// Checks whether the current user has the specified permission claim.
    /// Items without this permission are rendered visually disabled but remain visible (for debugging).
    /// </summary>
    /// <param name="permission">The GCP-style permission string to check.</param>
    /// <returns>True if the user holds the permission; otherwise false.</returns>
    private bool HasPermission(string permission)
    {
        return _user?.Claims.Any(c =>
            (c.Type == "permissions" || c.Type == "permission") &&
            c.Value == permission) ?? false;
    }

    private void HandleLogout()
    {
        Navigation.NavigateTo("/api/auth/logout", forceLoad: true);
    }
}
